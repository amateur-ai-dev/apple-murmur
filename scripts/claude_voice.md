Start the murmur voice daemon by running this shell command: `murmur start`

After running it, confirm it is running with `murmur status` and tell the user murmur is active — they can now double-tap fn anywhere to dictate.
